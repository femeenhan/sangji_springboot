<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.sangji.mapper.NoticeMapper">
    <select id="selectOne" parameterType="int" resultType="BoardDTO">
        SELECT NO, WRITER, TITLE, CONTENT, OFILE, NFILE, DATE (REGDATE) AS REGDATE, VISITCOUNT
        FROM NOTICE
        WHERE NO = #{NO}
    </select>

    <insert id="insert" parameterType="BoardDTO">
        INSERT INTO NOTICE (WRITER, TITLE, CONTENT, OFILE, NFILE, REGDATE, VISITCOUNT)
        VALUES (#{writer}, #{title}, #{content}, null, null, now(), 1)
    </insert>

    <update id="update">
        UPDATE NOTICE
        SET writer  = #{writer},
            title   = #{title},
            content = #{content},
            regdate = now()
        WHERE NO = #{NO}
    </update>

    <delete id="delete">
        DELETE
        FROM NOTICE
        WHERE NO = #{NO}
    </delete>

</mapper>
